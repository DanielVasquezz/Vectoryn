# torch: CPU-only build. In Dockerfile it's installed from the PyTorch index
# (saves ~2.5GB vs CUDA). In CI it's installed from PyPI (smaller CPU wheel).
# DO NOT remove — worker/embedder.py imports torch at module level.
torch>=2.2.0
confluent-kafka==2.3.0
qdrant-client==1.10.1
transformers==4.40.0
python-dotenv
certifi
numpy<2.0.0
fastembed>=0.2.0

# ── Observability ────────────────────────────────────────────
prometheus-client>=0.20.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
mock>=5.1.0
