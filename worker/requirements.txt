# torch: CPU-only build. En Dockerfile se instala desde el PyTorch index
# (ahorra ~2.5GB vs CUDA). En CI se instala desde PyPI.
torch>=2.2.0
confluent-kafka==2.3.0
qdrant-client==1.10.1
transformers==4.40.0
python-dotenv
certifi
numpy<2.0.0

# fastembed: SOLO se importa si ENABLE_SPARSE=true
# Mantener en requirements para que esté disponible, pero no se carga en startup
fastembed>=0.2.0

# ── Observability ────────────────────────────────────────────
prometheus-client>=0.20.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
mock>=5.1.0
