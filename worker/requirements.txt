# torch: CPU-only. Se instala desde PyTorch index en el Dockerfile
# ANTES que este requirements.txt, así pip no lo sobreescribe con CUDA.
torch>=2.2.0

confluent-kafka==2.3.0
qdrant-client==1.10.1
transformers==4.40.0
python-dotenv
certifi
numpy<2.0.0

# Prometheus: solo si ENABLE_PROMETHEUS=true (import condicional en código)
prometheus-client>=0.20.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
mock>=5.1.0
