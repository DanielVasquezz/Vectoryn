# ── v5.0: PyTorch ELIMINADO — usamos ONNX Runtime (~180MB menos RAM) ──────────
# torch: REMOVIDO — sentence-transformers con backend='onnx' no lo necesita
sentence-transformers>=2.7.0
onnxruntime>=1.17.0
optimum[onnxruntime]>=1.19.0

confluent-kafka==2.3.0
qdrant-client==1.10.1
transformers==4.40.0
python-dotenv
certifi
numpy<2.0.0

# fastembed: SOLO si ENABLE_SPARSE=true (no se importa en startup)
# fastembed>=0.2.0

# Prometheus: SOLO si ENABLE_PROMETHEUS=true
prometheus-client>=0.20.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
mock>=5.1.0
