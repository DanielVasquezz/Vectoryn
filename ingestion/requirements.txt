fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
confluent-kafka==2.3.0
slowapi
python-dotenv
numpy<2.0.0

# ── Observability ─────────────────────────────────────────────
prometheus-fastapi-instrumentator>=6.0.0
opentelemetry-sdk>=1.20.0
opentelemetry-api>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0

# ── Testing ───────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.24.0
mock>=5.1.0

# ── Tokenizer only (NO torch, NO sentence-transformers) ───────
# tokenizers: tokenización rápida en Rust — pesa ~4MB vs ~2GB de torch
# transformers: solo se usa AutoTokenizer, no necesita torch para eso
tokenizers>=0.19.0
transformers>=4.40.0